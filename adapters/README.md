# External data adapters

Reserved for Iteration 2+ source adapters (e.g. REData/e·sios/PVGIS) that update versioned snapshots. Adapter code must never become a runtime dependency of the core energy engine.
